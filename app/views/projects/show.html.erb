

<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @project.name %>
        <%= @project.source_lang_id %>
      </h1>
    </section>
  </aside>
</div>

<h2> My Translations </h2>

<%= render @translations %>

<%= link_to 'New', new_user_project_translation_path(current_user, @project), :class => 'btn btn-primary' %>
<%= link_to 'Batch Entry', '#', :class => 'btn btn-primary' %>